/* eslint import/prefer-default-export: "off" */
export const RGBA = {
  rIdx: 0,
  gIdx: 1,
  bIdx: 2,
  aIdx: 3,
};
Object.freeze(RGBA);

export const RGB = {
  rIdx: 0,
  gIdx: 1,
  bIdx: 2,
};
Object.freeze(RGB);

export const BGRA = {
  rIdx: 2,
  gIdx: 1,
  bIdx: 0,
  aIdx: 3,
};
Object.freeze(BGRA);

export const BGR = {
  rIdx: 2,
  gIdx: 1,
  bIdx: 0,
};
Object.freeze(BGR);
